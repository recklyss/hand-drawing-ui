name: hand-drawing-ui

on:
  push:
    branches:
      - main

jobs:
  test-build-package:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v4
      - name: Setup node env 🏗
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm ci
      - name: Run unit tests
        run: echo "pretend running tests"
      - name: Build
        run: echo "pretend running build"
      - name: package
        run: echo "pretend running package"

